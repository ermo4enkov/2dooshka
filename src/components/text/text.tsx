import * as React from 'react';
import SFC = require("react");

interface textProps{}
function textExample(props: textProps): JSX.Element {
    return (
        <div>sda</div>
    );
};


export default textExample;